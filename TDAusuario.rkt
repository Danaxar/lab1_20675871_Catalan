; TDA USUARIO

; Representación
; Constructores
(define user
    (lambda(usuario contrasena)
        ; Cuerpo de la función
        ()
    )
)

; Pertenencia
; Selectores
; Modificadores
; Otras operaciones
    



(define do-it 
    (lambda (a b) 
        (write “Hello”) (newline) (+ a b)
    )
)